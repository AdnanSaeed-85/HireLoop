"use client"

import Link from "next/link"
import { useRouter } from "next/navigation"
import { useState } from "react"
import { AnimatePresence, motion } from "framer-motion"
import { ArrowRight, Check, Eye, EyeOff, FileUp, LoaderCircle, Sparkles } from "lucide-react"
import { Logo } from "@/components/logo"
import { jobs } from "@/lib/demo-data"

export function AuthPage({ mode }: { mode: "login" | "register" }) {
 const router=useRouter();const [show,setShow]=useState(false);const [loading,setLoading]=useState(false);const register=mode==="register";
 function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setTimeout(()=>router.push("/dashboard"),700)}
 return <main className="relative flex min-h-screen overflow-hidden bg-background"><div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_20%_20%,color-mix(in_oklab,var(--primary)_12%,transparent),transparent_35%),radial-gradient(circle_at_90%_80%,color-mix(in_oklab,var(--primary)_9%,transparent),transparent_30%)]"/><section className="relative flex w-full items-center justify-center p-5 lg:w-[55%]"><div className="w-full max-w-md"><Logo/><div className="mt-12"><p className="text-xs font-extrabold uppercase tracking-[.18em] text-primary">{register?"Create your workspace":"Welcome back"}</p><h1 className="mt-3 text-balance text-3xl font-extrabold tracking-tight sm:text-4xl">{register?"Build a better hiring loop.":"Pick up where your team left off."}</h1><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{register?"Start hiring with clarity, speed, and a little help from AI.":"Sign in to review candidates, open roles, and key decisions."}</p></div><form onSubmit={submit} className="mt-8 flex flex-col gap-4">{register&&<label className="text-sm font-bold">Full name<input required className="mt-2 w-full rounded-xl border bg-card px-3 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="Sam Lee"/></label>}<label className="text-sm font-bold">Work email<input required type="email" defaultValue={register?"":"sam@hireloop.demo"} className="mt-2 w-full rounded-xl border bg-card px-3 py-3 font-normal outline-none focus:ring-2 focus:ring-ring" placeholder="you@company.com"/></label><label className="text-sm font-bold">Password<span className="relative mt-2 flex"><input required minLength={6} type={show?"text":"password"} defaultValue={register?"":"hireloop"} className="w-full rounded-xl border bg-card px-3 py-3 pr-11 font-normal outline-none focus:ring-2 focus:ring-ring"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3" aria-label={show?"Hide password":"Show password"}>{show?<EyeOff className="size-5"/>:<Eye className="size-5"/>}</button></span></label><button disabled={loading} className="mt-2 flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-extrabold text-primary-foreground shadow-sm disabled:opacity-70">{loading?<LoaderCircle className="size-4 animate-spin"/>:<>{register?"Create workspace":"Sign in"}<ArrowRight className="size-4"/></>}</button></form><p className="mt-6 text-center text-sm text-muted-foreground">{register?"Already have an account? ":"New to HireLoop? "}<Link className="font-bold text-primary" href={register?"/login":"/register"}>{register?"Sign in":"Create an account"}</Link></p></div></section><aside className="relative hidden flex-1 items-center justify-center bg-primary p-12 text-primary-foreground lg:flex"><div className="max-w-md"><span className="flex size-12 items-center justify-center rounded-2xl bg-card/15"><Sparkles className="size-6"/></span><blockquote className="mt-8 text-balance text-3xl font-extrabold leading-snug">“HireLoop helped us turn a messy recruiting process into one clear, confident system.”</blockquote><p className="mt-6 text-sm font-bold">Priya Rao · VP People at Northstar</p><div className="mt-10 flex gap-2">{[0,1,2].map(x=><span key={x} className={x===0?"h-1.5 w-8 rounded-full bg-card":"size-1.5 rounded-full bg-card/40"}/>)}</div></div></aside></main>
}

export function ApplyPage(){const [step,setStep]=useState<"form"|"sending"|"done">("form");function submit(e:React.FormEvent){e.preventDefault();setStep("sending");setTimeout(()=>setStep("done"),1200)}return <main className="min-h-screen bg-muted/40"><header className="border-b bg-card"><div className="mx-auto flex max-w-5xl items-center justify-between px-5 py-4"><Logo/><Link href="/login" className="text-sm font-bold text-primary">Recruiter sign in</Link></div></header><div className="mx-auto max-w-3xl px-5 py-12"><AnimatePresence mode="wait">{step==="done"?<motion.section initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} className="rounded-3xl border bg-card p-10 text-center shadow-sm"><span className="mx-auto flex size-16 items-center justify-center rounded-2xl bg-primary text-primary-foreground"><Check className="size-8"/></span><h1 className="mt-6 text-3xl font-extrabold">Application received</h1><p className="mx-auto mt-3 max-w-md text-sm leading-relaxed text-muted-foreground">Thanks for applying. Our team will review your experience and be in touch soon.</p><button onClick={()=>setStep("form")} className="mt-7 rounded-xl border px-4 py-2.5 text-sm font-bold">Submit another application</button></motion.section>:<motion.section initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} className="rounded-3xl border bg-card p-6 shadow-sm sm:p-9"><p className="text-xs font-extrabold uppercase tracking-[.18em] text-primary">Join the loop</p><h1 className="mt-3 text-3xl font-extrabold">Apply to Northstar</h1><p className="mt-2 text-sm text-muted-foreground">Tell us a little about yourself and the work you’d love to do.</p><form onSubmit={submit} className="mt-8 grid gap-5 sm:grid-cols-2"><label className="text-sm font-bold sm:col-span-2">Role<select required className="mt-2 w-full rounded-xl border bg-background px-3 py-3 font-normal">{jobs.filter(j=>j.status==="Active").map(j=><option key={j.id}>{j.title}</option>)}</select></label><label className="text-sm font-bold">First name<input required className="mt-2 w-full rounded-xl border bg-background px-3 py-3 font-normal outline-none focus:ring-2 focus:ring-ring"/></label><label className="text-sm font-bold">Last name<input required className="mt-2 w-full rounded-xl border bg-background px-3 py-3 font-normal outline-none focus:ring-2 focus:ring-ring"/></label><label className="text-sm font-bold sm:col-span-2">Email<input required type="email" className="mt-2 w-full rounded-xl border bg-background px-3 py-3 font-normal outline-none focus:ring-2 focus:ring-ring"/></label><label className="sm:col-span-2 flex cursor-pointer items-center gap-3 rounded-2xl border border-dashed p-5 text-sm"><span className="flex size-10 items-center justify-center rounded-xl bg-primary/8 text-primary"><FileUp className="size-5"/></span><span><strong className="block">Upload your resume</strong><span className="text-xs text-muted-foreground">PDF or DOCX, up to 5 MB</span></span><input required type="file" accept=".pdf,.doc,.docx" className="sr-only"/></label><button disabled={step==="sending"} className="sm:col-span-2 flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-extrabold text-primary-foreground">{step==="sending"?<><LoaderCircle className="size-4 animate-spin"/>Submitting application</>:<>Submit application<ArrowRight className="size-4"/></>}</button></form></motion.section>}</AnimatePresence></div></main>}
